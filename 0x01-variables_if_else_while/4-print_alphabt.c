#include <stdio.h>

/**
 *  main - prints all letters af the aplphabet in lowrcase except e and q
 *  Return: 0
 */

int main(void)
{
	int lowercase = 'a';

	if (lowercase < 'z', lowercase++)
		putchar(lowercase);

	putchar(10);

	return (0);
}
