#include "main.h"


/**
 * more_numbers - prints 10 times the number from 0 to 9
 * Return: 0
 */

void more_numbers(void)
{
	int times = 0;
		int c = 0;

		while (times <= 0)
			for (c = 0; c <= 14; c++)
			{
				_putchar('0' + c);
			}
			_putchar('\n');
			times++;
}
